DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/chyrplite
JWT_SECRET=change-me
ENVIRONMENT=development
REDIS_URL=rediss://:<UPSTASH_REDIS_PASSWORD>@<UPSTASH_REDIS_HOST>:<PORT>
# For Upstash, copy the "UPSTASH_REDIS_REST_URL" or "UPSTASH_REDIS_TLS_URL" from your Upstash dashboard and paste it here.
# Example: rediss://:password@us1-xxxxxx.upstash.io:port
BASE_URL=http://localhost:8000

# Example Neon (replace credentials):
# DATABASE_URL=postgresql+psycopg://neondb_owner:password@your-neon-host/neondb?sslmode=require&channel_binding=require

